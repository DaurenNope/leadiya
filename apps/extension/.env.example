# Optional: Chrome extension OAuth2 client ID (Google Cloud Console → APIs → OAuth client → Chrome extension).
# Enables Google Sheets sink. Build with: WXT_GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com npm run build
WXT_GOOGLE_CLIENT_ID=
