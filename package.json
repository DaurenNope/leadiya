{
  "name": "leadiya-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@10.8.2",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "vitest run",
    "benchmark:2gis": "npx tsx scripts/benchmark-2gis-scrape.ts",
    "test:stage2-http": "npx tsx scripts/test-stage2-http.ts"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "turbo": "^2.1.0",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.1"
  }
}
